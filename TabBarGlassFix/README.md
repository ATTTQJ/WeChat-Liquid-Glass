# WCLG TabBar Glass Fix

Companion tweak for the original Liquid Glass plugin.

It keeps plugin-created tab bar glass/platter views behind native `UITabBarButton` labels and icons, hides duplicate labels inside glass views, and pixel-aligns relevant frames to reduce text blur.

It does not modify the original dylib.

## Build

```sh
make package
```
